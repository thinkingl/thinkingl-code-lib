2020-07-25：
    1. 重构计划：
        1. 下载直接使用urllib，不再依赖浏览器
        2. 相关文件存两份，按月份分目录存一份。 按演员为目录存一份，只收录有名称的单人作品，不收录匿名演员作品，不收录多人作品或多人合集。
        3. 把当前文件重新导入。
        4. 所有文件信息存入数据库
        5. 数据库通过http-REST接口访问，可以通过代理访问
        6. 去重： 通过数据库来对作品页面去重，通过本地文件列表对列表页面去重， 这样本地的文件可以转移走。
        7. 数据库损坏风险，考虑是否支持同时写2个数据库。
